# Gacha Rare Import Folder

Rare 向け PNG を置くフォルダです。

- 正方形 `1024x1024` 以上の PNG を推奨します
- ファイル名は manifest の `pngFilename` と完全一致させてください
- import script は missing file を許容し、見つかった PNG だけを取り込みます
- 画像未追加の item は fallback preview のまま安全に表示されます
